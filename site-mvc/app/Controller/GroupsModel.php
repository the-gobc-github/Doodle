<?php

namespace App\Models;

use Core\Model\Model;
use App\App;

class GroupsModel extends Model{

	/* public function create_group() { */

	/* 		if($_SESSION['log'] != NULL AND $_POST['name'] != NULL) */
	/* 		{ */
	/* 				$login = $_SESSION['log']; */
	/* 				$name = $_POST['name']; */
	/* 				try { */

	/* 					echo 'ok'; */
	/* 						/1* $what = 'UserID'; *1/ */
	/* 						/1* $where = 'login'; *1/ */
	/* 						/1* $value = 'a'; *1/ */
	/* 						/1* $userid = Model::get_where($state); *1/ */
	/* 						/1* var_dump($user_id); *1/ */
	/* 						/1* $state = array("name"=>"X","UserID"=>1); *1/ */
	/* 						/1* $res = Model::insert_into($state); *1/ */
	/* 						/1* $res = $db->query('INSERT INTO users (login, password, valid) VALUES(:login, :password, :valid)', array( *1/ */
	/* 						/1* 		':login' => $login, *1/ */
	/* 						/1* 		':password' => $password, *1/ */
	/* 						/1* 		':valid' => $valid)); *1/ */
	/* 				} */
	/* 				catch(Exception $e) */
	/* 				{ */
	/* 						echo 'prout'; */
	/* 						die('Erreur : '.$e->getMessage()); */
	/* 				} */
	/* 		} */

	/* 		else { */
	/* 			echo 'not conform to the fields'; */
	/* 		} */

	/* } */
}
?>
